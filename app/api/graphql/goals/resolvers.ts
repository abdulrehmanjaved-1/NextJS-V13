const queries={
    getGoals: async()=>{
        return 'done hy boss'
    }
};
const mutations={
    addGoals: async(_,{})=>{
        return 'done hy boss'
    }
}

export const resolvers={queries,mutations}